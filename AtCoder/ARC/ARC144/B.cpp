#include <bits/stdc++.h>
using namespace std;

// max(A) >= min(A) + b のとき、min(A) を大きくするi, jは選べない

int main() {
  int n, a, b;
  cin >> n >> a >> b;
  vector<int> x(n);
  for (int i = 0; i < n; i++) {
    cin >> x[i];
  }
}
