#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m, q;
  cin >> n >> m >> q;
  vector<vector<int>> v(1e9, vector<int>(1e9,-1));
  for (int i=0; i<m; i++) {
    int a, b, c;
    cin >> a >> b >> c;
  }

  for (int i=0; i<q; i++) {
    int x, y, z, w;
    cin >> x >> y >> z >> w;
  }
}